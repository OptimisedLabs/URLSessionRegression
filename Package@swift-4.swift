// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "URLSessionRegression",
    dependencies: [
    ],
    targets: [
        .target(
            name: "URLSessionRegression",
            dependencies: []),
    ]
)
